package backend;

import java.io.File;

public class Save {

		public void SaveGame(){
		File myFile = new File(File.separator+"home"+File.separator+g.getplayer().getname()+".java");
		
		/*   se le debe consultar el 
		 *   nombre y la ubicación del archivo a guardar
		 */
		
		/*
		 * Utilizar serializacion.
		 */
		
		/* Luego, cuando el usuario restaura una jugada, deberá seleccionar el archivo que previamente
		guardó. La aplicación deberá cargar el tablero especificado en el archivo y continuar el juego
		desde allí.
		 */
		}
		
}
